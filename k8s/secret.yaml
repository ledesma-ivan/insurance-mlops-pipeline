# secret.yaml — template only. DO NOT commit real values.
# Create the real secret with:
#   kubectl create secret generic fraud-detection-secrets \
#     --from-literal=anthropic-api-key=YOUR_KEY
#
# Or apply this file after base64-encoding your key:
#   echo -n "sk-ant-..." | base64
apiVersion: v1
kind: Secret
metadata:
  name: fraud-detection-secrets
type: Opaque
data:
  anthropic-api-key: ""  # base64-encoded key goes here
